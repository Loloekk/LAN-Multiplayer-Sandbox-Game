/*
package io.github.terraria.logic.building.falling;

import com.badlogic.gdx.math.Vector2;
import io.github.terraria.logic.building.PhysicalBlock;

public class VolatileBlockImpl extends VolatileBlock {
    private Vector2 prvLoc;
    public VolatileBlockImpl(PhysicalBlock phy) {
        super(phy);
        prvLoc = body.getPosition();
    }

    @Override
    public boolean update() {
        boolean ret = prvLoc != body.getPosition();
        prvLoc = body.getPosition();
        return ret;
    }
}
*/
